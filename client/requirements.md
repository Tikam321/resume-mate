## Packages
framer-motion | Smooth animations for result reveals
lucide-react | Iconography for visual cues (Check, X, Alert, Copy)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
File uploads expect POST /api/analyze with multipart/form-data (resume, job_description)
